div.row
  .span8
    =link_to 'Link to the old candidate detail page', candidate_path({:id => @candidate.id, :legacy => true}), :style => "font-size:20px; color:red"
    =render 'candidate_description'
    =render 'show_latest_applying_job'
    =render 'candidate_assessments'
    =render 'recruiting_history'
  .span4
    =render 'candidate_profile'
